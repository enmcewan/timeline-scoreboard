{
  "name": "match-layout",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch:fixtures": "node --env-file=.env src/scripts/fetch-fixtures.mjs",
    "fetch:events": "node --env-file=.env src/scripts/fetch-events.byFixture.mjs",
    "refresh:epl2025": "node src/scripts/refresh-epl-2025.mjs",
    "refresh:all": "npm run fetch:fixtures && npm run fetch:events && npm run refresh:epl2025",
    "update:matchdays": "npm run fetch:events && npm run refresh:epl2025"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
